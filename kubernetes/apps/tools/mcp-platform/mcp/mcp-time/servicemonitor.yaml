---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mcp-time
  namespace: mcp-platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: mcp-time
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
