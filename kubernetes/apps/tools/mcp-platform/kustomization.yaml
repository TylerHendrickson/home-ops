---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./ks.yaml
  # TODO: add ./shared/kustomization.yaml when first cross-MCP resource lands (MCPExternalAuthConfig, default-deny NetworkPolicy)
