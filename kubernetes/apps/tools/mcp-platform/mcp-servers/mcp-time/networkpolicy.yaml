---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-time-egress-deny
  namespace: mcp-platform
spec:
  podSelector:
    matchLabels:
      toolhive-name: mcp-time
  policyTypes:
    - Egress
  egress:
    - {}
