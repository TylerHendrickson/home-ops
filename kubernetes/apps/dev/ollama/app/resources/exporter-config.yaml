---
ollama_url: "http://localhost:80"
listen_addr: ":9400"
poll_interval: "15s"
log_level: "info"

proxy:
  enabled: true
  listen_addr: ":9401"
  exclude_paths:
    - "/"
    - "/api/ps"
    - "/api/tags"
    - "/api/show"
    - "/api/version"

gpu:
  enabled: false
